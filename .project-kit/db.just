# .project-kit/db.just — generated for drape (backend=none)
# Generated by project-kit (skill version: 0.1.0)
# Last refreshed: 2026-06-06

db-migrate:
    @just _skip "db-migrate"

db-rollback:
    @just _skip "db-rollback"

db-fresh:
    @just _skip "db-fresh"

db-shell:
    @just _skip "db-shell"

db-dump:
    @just _skip "db-dump"

db-restore FILE:
    @just _skip "db-restore"

db-seed:
    @just _skip "db-seed"
